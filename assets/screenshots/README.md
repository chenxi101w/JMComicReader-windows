# 截图放置目录

把以下三张仓库页面预览图放在本目录，然后在 `README.md` 的「📸 预览」小节取消 HTML 注释即可显示：

| 文件名 | 内容 |
|--------|------|
| `home.png` | 主页（智能推荐） |
| `search.png` | 搜索结果（多维度） |
| `reader.png` | 阅读器（滚轮缩放） |

建议：PNG 格式、宽度约 760px、单张 < 500KB。详见仓库根目录 `GITHUB_SETUP.md`。
